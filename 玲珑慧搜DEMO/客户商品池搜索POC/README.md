# 客户商品池搜索 POC

本目录根部的 `index.html` 与 `assets/` 为 GitHub Pages 发布产物；`prototype/` 保存可维护的 React 源码。

应用纯浏览器运行：Excel 仅在访问者本地解析，模板下载、内嵌图片解析与本地智能检索均不依赖后端服务。

更新发布文件：进入 `prototype/` 后运行 `npm.cmd run build:pages`。
